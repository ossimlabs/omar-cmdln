pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-cmdln-app'

project(':omar-cmdln-app').projectDir = "${rootProject.projectDir}/apps/omar-cmdln-app" as File
